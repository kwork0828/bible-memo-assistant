# AI Provider
AI_PROVIDER=
OPENAI_API_KEY=여기에 본인 키
OPENAI_BASE_URL=
OPENAI_MODEL=실제 모델 목록 조회 후 입력

# Firebase
# 로컬: backend/firebase-service-account.json 같은 파일 경로
# 배포: 서비스 계정 JSON 문자열
FIREBASE_SERVICE_ACCOUNT_JSON=backend/firebase-service-account.json

# CORS - 여러 주소는 쉼표로 구분
ALLOWED_ORIGINS=http://localhost:3000
